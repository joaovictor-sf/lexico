declaracoes
    inteiro x, y, z;
    real resultado;
fimDeclaracoes

funcoes
    tipoFunc vazio calcula()
        inteiro contador := 0;
        enquanto contador < 10
            resultado := resultado + contador * 2.5;
            contador := contador + 1;
        fimEnquanto
        retorna;
    fimFunc
fimFuncoes

    x := 10;
    y := 20;
    z := x + y;
    imprime(x, y, z, resultado);
fimPrograma
